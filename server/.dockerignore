# Dependencies and build artifacts (reinstalled / rebuilt inside the image)
node_modules
dist
coverage

# Local environment and databases (never bake these into the image)
.env
.env.*
*.db
*.db-journal

# Tooling / VCS / docs not needed at build time
.git
.gitignore
Dockerfile
.dockerignore
*.log
